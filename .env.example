# GitHub OAuth — used only for displaying the user's GitHub avatar + name
# in the sidebar. Register an OAuth App at:
#   https://github.com/settings/developers
# Authorization callback URL during local dev (https via Next.js
# --experimental-https):
#   https://localhost:3000/api/auth/github/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=
